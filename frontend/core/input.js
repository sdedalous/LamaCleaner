// =============================================================
// INPUT HANDLING (DISABLED FOR MASK; MASK.JS OWNS BRUSH INPUT)
// =============================================================

export function attachInputHandlers(maskCanvas) {
    // Intentionally left blank.
    // Mask input is handled exclusively in mask.js (setupMask).
}
