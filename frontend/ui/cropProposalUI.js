// =============================================================
// CROP PROPOSAL UI (to be filled when cropping is integrated)
// =============================================================

export function setupCropProposalUI() {
    // will be filled later
}
